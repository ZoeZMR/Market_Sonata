# Copy this file to backend/.env and fill in your key to enable the
# Claude-polished Analyst's Note. Without it, the app falls back to the
# grounded rule-based narration automatically — see backend/analyst.py.
ANTHROPIC_API_KEY=
